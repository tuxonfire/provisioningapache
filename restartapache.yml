---
- name: Install Apache playbook
  hosts: 18.218.53.106  
  become: yes
  gather_facts: false
  vars:
    ansible_become_flags: '-H -S -n -i'

  tasks:
  - name: restart apache service
    service:
      name: httpd
      state: restarted
      enabled: yes
